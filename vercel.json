{
  "buildCommand": "next build",
  "env": {
    "NODE_TLS_REJECT_UNAUTHORIZED": "0"
  },
  "build": {
    "env": {
      "NODE_TLS_REJECT_UNAUTHORIZED": "0"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
